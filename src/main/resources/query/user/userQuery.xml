<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">

<mapper namespace="com.youngsuk.bookstore.repository.UserRepository">

    <insert id="insertUserData">
        INSERT INTO user (userId, userPassword, userName, userEmail)
        VALUES (#{userId},#{userPassword},#{userName},#{userEmail})
    </insert>

    <delete id="deleteUserData">
        DELETE FROM user
        WHERE userId = (#{userId})
    </delete>

    <select id="selectUserPassword" resultType="string">
        SELECT userPassword FROM user
        WHERE userId = (#{userId})
    </select>
</mapper>
